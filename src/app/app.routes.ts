import { Routes } from '@angular/router';
import { PrincipalComponent } from './observable/principal/principal.component';  
export const routes: Routes = [
    { path: 'observable', component: PrincipalComponent },
];
